services:
  - type: worker
    name: sistema-websocket-worker
    env: php
    plan: free
    buildCommand: cd backend && composer install --no-dev --optimize-autoloader
    startCommand: cd backend && php public/ws-server.php
